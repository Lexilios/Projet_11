using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/*public class scoring : MonoBehaviour
{
    public BoxCollider footballgoalPlayer1Collider;

    // Points attribués lorsque la balle touche le BoxCollider du footballgoalPlayer1
    public int scoreValue = 1;

    // Méthode appelée lorsqu'un objet entre en collision avec le BoxCollider
    private void OnTriggerEnter(Collider other)
    {
        // Vérifie si l'objet entrant est la balle
        if (other.CompareTag("Ball"))
        {
            // Vérifie si la balle touche le BoxCollider du footballgoalPlayer1
            if (footballgoalPlayer1Collider.bounds.Contains(other.transform.position))
            {
                // Augmente le score
                GameManager.instance.AddScore(scoreValue);
            }
        }
    }
}
*/
